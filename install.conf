# Shared installation configuration
# This file is sourced by setup shell scripts and can also be read by Python tools.
# MODEL_NAME is the runtime default when a task does not request a model.
# Models are installed after setup from the web UI at /add-remove-components.

AI_USER="aiuser"
INSTALL_DEST_DIR="local-ai-agent"

# Small default for lower-memory VMs.
MODEL_NAME="qwen2.5-coder:1.5b"

# Medium options.
# MODEL_NAME="qwen2.5-coder:3b"
# MODEL_NAME="deepseek-coder:1.3b"

# Larger default options. These need more RAM/CPU when installed.
# MODEL_NAME="deepseek-coder:6.7b-instruct-q4_K_M"
# MODEL_NAME="qwen2.5-coder:7b"
# MODEL_NAME="codellama:7b"

LOG_LEVEL="INFO"
